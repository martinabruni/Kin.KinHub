---
type: changed
area: infrastructure
breaking: true
issue: none
---

## it
FEAT-016 ora deriva in modo deterministico i nomi Azure del solo ambiente `dev`, sostituisce PostgreSQL/Npgsql con Azure SQL Database Basic e aggiorna backend, migration, workflow, skill e runbook al nuovo contratto identity-based.

## en
FEAT-016 now derives the `dev` Azure resource names deterministically, replaces PostgreSQL/Npgsql with Azure SQL Database Basic, and updates the backend, migrations, workflows, skills, and runbooks to the new identity-based contract.
