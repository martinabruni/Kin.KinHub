---
type: fixed
area: shared
breaking: false
issue: none
---

## it
Corretta la pagina Famiglia: il refresh non approvato è stato rimosso dalla card del nome e il membro autenticato usa lo stesso nome profilo mostrato dal menu utente, mantenendo il fallback per i nomi assenti.

## en
Fixed the Family page: the unapproved refresh action was removed from the family name card, and the signed-in member now uses the same profile name shown by the user menu while preserving the fallback for missing names.
